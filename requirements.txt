streamlit>=1.28.0
numpy>=1.24.0
pandas>=2.0.0
pillow>=10.0.0
plotly>=5.17.0
requests>=2.31.0

# OpenAI (オプション)
openai>=0.28.0

# OpenCV (軽量版を使用)
opencv-python-headless>=4.8.0

# WebSocket (オプション - 必要な場合のみ)
# websocket-client>=1.6.0

# OBS連携 (ベータ版では無効)
# obs-websocket-py>=0.5.3

# GPT-OSS (オプション - インストールできない場合は除外)
# gpt-oss>=0.0.6
# openai-harmony>=0.1.0

# Ollama (オプション - インストールできない場合は除外)
# ollama>=0.1.0
